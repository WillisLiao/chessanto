.DS_Store
.build/
.swiftpm/
DerivedData/
*.xcuserstate
xcuserdata/

# Chessanto.xcodeproj is generated by `xcodegen generate` from project.yml -
# don't commit it, regenerate it instead.
Chessanto.xcodeproj/

# Stockfish NNUE networks (75MB) - fetched by scripts/fetch-nnue.sh, which
# verifies them against the sha256 prefix embedded in their filenames.
*.nnue
handoffs/db-backups/
