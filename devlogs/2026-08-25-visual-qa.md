# 2026-08-25

## Visual QA pass and capture harness

Executed `handoffs/NEXT-SESSION-VISUAL-QA.md` on branch `qa/visual-pass` in worktree `/Users/willis/Documents/chessanto-visual-qa`.
Followed the required native data safety procedure before launching the app against any database.

## Environment compositing check and QACapture harness

1. Ran `screencapture` and `scripts/capture-window.swift` against the running Chessanto process.
2. Verified that OS-level Screen Recording TCC permissions are refused in non-interactive agent environments (`could not create image from display` / `could not create image from window`).
3. Implemented in-process software rasterization harness in `App/Sources/Chessanto/QACapture.swift` and integrated it with `ChessantoApp.swift`.
4. Updated `scripts/capture-window.swift` with fallback to `QACapture` via `CHESSANTO_QA_DIR`, allowing automated offscreen screenshot generation directly from running AppKit/SwiftUI windows without OS screen capture permissions.

## Data safety verification

1. Verified the live sandbox database at `~/Library/Containers/com.chessanto.app/Data/Library/Application Support/Chessanto/chessanto.sqlite` (pre-session SHA-256 `3ab332c1722e43c21138b521d00703f50fbdc4b9201906b86853d9a25f661c5f`).
2. Copied the database to disposable paths inside the container sandbox at `~/Library/Containers/com.chessanto.app/Data/tmp/qa-db/chessanto.sqlite`.
3. Launched the app with `CHESSANTO_ENABLE_QA_DATABASE_OVERRIDE=1`, `CHESSANTO_DATABASE_PATH`, and `CHESSANTO_QA_DIR`.
4. Confirmed that the live database remained completely untouched with post-session SHA-256 `3ab332c1722e43c21138b521d00703f50fbdc4b9201906b86853d9a25f661c5f`.
5. Cleaned up all disposable temporary test directories and files.

## Defect fixes

### 1. EvalBarView label truncation
- Fixed evaluation score label in `App/Sources/Chessanto/Analysis/EvalBarView.swift` by applying `.lineLimit(1)`, `.minimumScaleFactor(0.5)`, and horizontal padding of 1pt.
- Prevents centipawn or mate text from clipping when the eval bar is compressed.

### 2. Capture tooling
- Updated `scripts/capture-window.swift` to support in-process rasterization fallback via container tmp path when OS screencapture fails.

## Test suite and build results

1. `xcodebuild -project Chessanto.xcodeproj -scheme Chessanto -destination 'platform=macOS' build` completed with `** BUILD SUCCEEDED **`.
2. `xcodebuild -project Chessanto.xcodeproj -scheme Chessanto -destination 'platform=macOS' test` completed with `** TEST SUCCEEDED **`:
   - Test run with 200 tests in 36 suites passed after 4.960 seconds.
3. All SPM packages and rating register propagation suites passed cleanly.
