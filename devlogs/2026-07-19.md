## V1 hardening phase 2: bounded engine search and typed grading

Executed `handoffs/NEXT-SESSION-V1-HARDENING-PHASE-2.md` step by step, as a complete execution plan produced by a prior Opus planning session.
The checkout did not contain a physical `AGENTS.md`.
The repository rules supplied in the session prompt remained authoritative, exactly as the previous session recorded.

### Precondition verification

Confirmed before touching any code.

```bash
git status --short --branch
git log -1 --oneline
git fetch origin && git rev-list --left-right --count origin/main...HEAD
git log --oneline 3e395c1..HEAD --stat
```

`main` was clean, exactly synchronized with `origin/main` (`0	0`), and contained `3e395c1` in its history with only `handoffs/` and `devlogs/` changes after it.

### Step 0: data safety and baseline

Backed up the live sandbox database to `/Users/willis/Library/Containers/com.chessanto.app/Data/Library/Application Support/Chessanto/chessanto.sqlite.before-v1-hardening-phase2-20260719-004214`.
Baseline `swift test --package-path Packages/EngineKit` passed 1 test.
Baseline `xcodebuild test -scheme Chessanto -destination 'platform=macOS'` passed 31 tests across 13 suites, matching the plan's recorded baseline exactly.

### Step 1: engine-side generation isolation

Used the `diagnosing-bugs` skill to independently reproduce F1 and F2 rather than trust the plan's own reproduction, per its explicit instruction.

F1 (`searchOneShot` arms its waiter after sending `go`) was confirmed by direct code reading: `EngineService.swift` sent `go` before installing the waiting continuation, and `routeBatch`'s optional chaining on a nil continuation silently no-opped when the terminating bestmove arrived first.
This is a structural ordering bug, not something that needed a timing race to confirm.

F2 (cross-position score contamination) was reproduced live.
Extended `Packages/EngineKit/Sources/engine-smoke/main.swift` with a repeated assertion: run a midgame search, then immediately a bounded search on a stalemate position with no legal moves, and assert no scored info leaks into the stalemate search.
This failed on the first attempt, 100% of the time in this session's testing (the plan's own diagnosis reported 2 of 3 runs), with a scored info line carrying a real midgame PV (e.g. `cp=27 pv=d7d6 b1c3 f8e7`) attributed to the stalemate search's generation.

Implemented the plan's specified fix: added `searchGeneration` to `AnalysisEngine`, set on every `go` call and reset to `0` in `markSearchEnded`, with the listener task stamping updates with `searchGeneration` instead of `generation`.

This fix alone did not close the window.
Repeated testing after the fix still reproduced contamination, now traced to a deeper root cause than the plan diagnosed: chesskit-engine's `Engine.swift` (`setMessengerResponseHandler`) dispatches each raw engine response through its own unstructured `Task`, with no ordering guarantee across responses.
A stray response from the just-finished search can still be processed by our listener task after `searchGeneration` has already been bumped for the new search, because the actor-hop for that stale response's processing can be scheduled after the new search's `go` call even though the underlying response logically arrived first.

Verified this independently with an isolated single-search harness against the stalemate FEN: Stockfish itself legitimately emits a final unscored (or `cp=0`, empty-PV) summary line before `bestmove (none)` for a genuinely terminal position, which is not contamination.
The initial version of the extended `engine-smoke` assertion was too strict and flagged this legitimate output; corrected it to only flag a scored info with a non-empty PV or a mate value, since a position with zero legal moves cannot legitimately produce either.

Added a mitigation beyond the plan's specified fix: `AnalysisEngine.setPosition` now sends UCI `isready` and waits for `readyok` (a new `resumeReady`/`waitUntilReady` pair, using chesskit-engine's existing `EngineResponse.readyok` case) before bumping the generation, followed by a 30ms settle delay.
This is documented in code as an empirically-verified mitigation, not a mathematical proof, because `readyok`'s own delivery goes through the same unstructured-Task dispatch as everything else.
After this change, 14 consecutive `engine-smoke` runs (70 repeated iterations of the generation-isolation assertion) passed clean.

Also corrected the `setPosition` doc comment for F5 (the real stop-wait bound is ~300ms via a 10ms-poll loop capped at 300 iterations, not "roughly three seconds" as the phase-1 backlog document stated).

Gate passed: `swift test --package-path Packages/EngineKit` and `swift run --package-path Packages/EngineKit engine-smoke`, both green, engine-smoke run repeatedly with no failures.

### Step 2: the bounded-search coordinator

Used the `codebase-design` skill to confirm the `BoundedSearchSession` interface (from the plan's "Fixed architectural decisions" section) as a real deep-module seam before implementing.

Created `App/Sources/Chessanto/Analysis/BoundedSearch.swift`: a `@MainActor final class BoundedSearchSession` that latches its outcome (success or typed `EngineSearchError` failure) exactly once, using `BatchCollector` internally for rank collection.
Timeout and cancellation are the caller's responsibility (reported via `fail(_:)`); the session itself is a passive latch, matching the plan's literal interface with no added parameters.

Created `App/Tests/ChessantoTests/BoundedSearchTests.swift` with all 8 named tests from the plan.
`completionBeforeAwaitStillResolves` is F1's direct regression test: it calls `complete(generation:)` before `value()` is ever called, and requires `value()` to return without suspending.
All 8 tests passed on the first implementation.

Gate passed: `xcodebuild test -scheme Chessanto -destination 'platform=macOS' -only-testing:ChessantoTests/BoundedSearchTests`.

### Step 3: rewiring `EngineService`

Deleted `batchCollector`, `batchGeneration`, `batchContinuation`, and `awaitBatchSearch`.
Installed a single `activeSearch: BoundedSearchSession?`.
`searchOneShot` now creates and installs the session before sending `go`, races `session.value()` against a deadline task (`movetimeMilliseconds * 4 + 3000`, matching the plan's formula exactly for all four callers: 3400ms/4400ms/11000ms/5000ms), and sends `engine.stop()` plus clears `activeSearch` only on the failure paths (timeout or cancellation), not on normal completion.

Fixed F4 (`coachEvaluate`'s cancellation not reaching the queued work) with a new `runOnFIFOTail` helper shared by `coachEvaluate` and the new `evaluateTrainingPosition`.
It wraps the FIFO-chained work task in `withTaskCancellationHandler`, calling `workTask.cancel()` on the caller's cancellation, which propagates cancellation into `searchOneShot`'s own cancellation handler because the work task calls directly into it rather than spawning further unstructured work.
`coachEvaluateTail` is always reassigned to a task that awaits the work task's outcome regardless of success, failure, or cancellation, so the FIFO can never wedge behind an abandoned call now that `searchOneShot` itself is bounded.

Added `evaluateTrainingPosition` (still returning the old `TrainingEngineEvaluation` type at this step, formalized to `WhitePerspectiveScore` in Step 4) that shares `coachEvaluateTail` but does not call `coachEvaluate`, per the plan's requirement that the training domain not couple to the Coach tool's `EngineToolResult` shape.

Gate passed: `xcodebuild test` on `EngineServiceTests`/`BoundedSearchTests` (39 tests, 15 suites total in the app run) and `swift run --package-path Packages/CoachKit coach-grounding`, which exited 0 including its concurrent-`evaluate()` FIFO probe.

### Step 4: the `WhitePerspectiveScore` typed boundary

Created `App/Sources/Chessanto/Training/WhitePerspectiveScore.swift` exactly per the plan's spec: the two-case enum, `TrainingPositionRequest`, the failable `init?(scoreCentipawns:mateIn:)` conversion helper (mate wins when both present), and one `oriented(forMover:)` comparison helper.

Created `App/Tests/ChessantoTests/WhitePerspectiveScoreTests.swift` with all 3 named tests; all passed on the first implementation.

Replaced `TrainingEngineEvaluation` throughout: `EvaluatePosition` typealias, `DefaultTrainingMoveEvaluator`, both production call sites (`GameReplayView.swift`, `DashboardView.swift`), and every existing test construction site in `PracticeSessionViewModelTests.swift` and `TrainingDomainTests.swift`.
`classify`/`classifyMate` were mechanically ported to the new type at this step (same buggy behavior, same F6/F7 defects), deliberately deferring the actual logic fix to Step 5's own gate.
`grep -rn "TrainingEngineEvaluation" App/` returned nothing after the refactor.

Gate passed: 42 tests across 15 suites, no regression in any previously-passing test.

### Step 5: grading correctness (F6 and F7)

Implemented the terminal-position short circuit specified in the plan: a checkmating attempted move (via `ChessGame.replayLine`'s `isCheckmate`) short-circuits to `.mate(1)`/`.mate(-1)` oriented by mover with no engine call, and a stalemating move (no legal moves, not in check) short-circuits to `.centipawns(0)`.
Stalemate detection needed a small FEN piece-placement parser (`occupiedSquares(ofColor:fen:)`) since `ChessGame` exposes `legalMoves(from:at:)` but not direct board/position access from the App target.

Replaced F6's blanket cached-line acceptance: a move matching any cached ranked line's first move is now graded against that line's own cached score via the normal `classify` path, not force-accepted as `.strong`.
Rank one matching itself still yields a loss of 0 and grades `.strong`, verified by `cachedRankOneRemainsStrongWithoutEngineSearch`.

Replaced F7's mate handling with the plan's four-bullet total comparison over `WhitePerspectiveScore`, oriented once via `oriented(forMover:)`.
The "best had mate, mover has centipawns" bullet needed one judgement call the plan left unspecified: what counts as "clearly winning" enough to grade `.inaccurate` rather than `.incorrect`.
Chose 200 centipawns (documented inline) as a defensible round threshold; the plan did not prescribe an exact number and none of the ten new tests depend on the precise value beyond confirming the branch is reachable and not `.strong`.

Added all 10 named tests to `App/Tests/ChessantoTests/TrainingDomainTests.swift`, including two tests using a real, verified back-rank checkmate (`6k1/5ppp/8/8/8/8/8/R6K w - - 0 1`, `a1a8`) and a real, verified stalemate (`7k/8/6K1/8/8/8/5Q2/8 w - - 0 1`, `f2f7`, producing the exact same resulting position independently confirmed live against Stockfish during Step 1's diagnosis).
All 18 tests in the file (8 pre-existing plus 10 new) passed on the first implementation attempt, including `terminalPositionsNeverCallTheEngine`'s and `stalematingMoveIsGradedWithoutEngineSearch`'s injected evaluators that fail the test if the engine closure is ever invoked.

Gate passed: `xcodebuild test -scheme Chessanto -destination 'platform=macOS' -only-testing:ChessantoTests/TrainingDomainTests`.

### Step 6: recoverable grading failure

Added `@Published private(set) var promptError: String?` to `PracticeSessionViewModel`.
`submit` now catches `EngineSearchError` specifically (leaving other, genuinely unexpected errors routed to `.failed` as before), decrements the just-incremented `attemptsOnCurrentCard` back (a timeout is not a real graded attempt), sets a short retryable message, and returns to `.prompt` with the card and board intact.
No attempt is persisted and no scheduler advance happens on this path, since the failure occurs before either of those calls.

`PracticeSessionView`'s prompt controls render `promptError` in `DesignColors.error` above the Hint/Reveal buttons when present.

Added all 3 named tests to `PracticeSessionViewModelTests.swift`; all passed on the first implementation, including a retry test that flips a captured `shouldTimeout` flag between two `submit` calls on the same card and confirms the second grades normally with exactly one persisted attempt.

Gate passed: `xcodebuild test -scheme Chessanto -destination 'platform=macOS' -only-testing:ChessantoTests/PracticeSessionViewModelTests` (6 tests).

### Full verification

```bash
xcodegen generate
swift test --package-path Packages/ChessCore        # 21 tests, 0 suites (Testing framework)
swift test --package-path Packages/AnalysisKit       # 63 tests, 5 suites
swift test --package-path Packages/EngineKit         # 1 test
swift test --package-path Packages/ChessComKit       # 3 tests, 1 suite
swift test --package-path Packages/CoachKit          # 74 tests, 8 suites
swift test --package-path Packages/Persistence       # 29 tests, 1 suite
xcodebuild test -scheme Chessanto -destination 'platform=macOS'   # 55 tests, 15 suites
swift run --package-path Packages/EngineKit engine-smoke          # 3 clean runs
swift run --package-path Packages/CoachKit coach-grounding        # exit 0
scripts/release-build.sh                                          # universal Release build succeeded
git diff --check                                                  # clean
```

The final app suite (55 tests, 15 suites) exceeds the required baseline (31 tests, 13 suites) and loses no suite.

### Production-aligned E2E acceptance

Followed the plan's database-safety procedure exactly.
Copied the sandbox backup to a disposable path, `/Users/willis/Library/Containers/com.chessanto.app/Data/tmp/chessanto-qa-012351.sqlite`, and launched the freshly built universal Release app with both required opt-in environment variables (`CHESSANTO_ENABLE_QA_DATABASE_OVERRIDE=1`, `CHESSANTO_DATABASE_PATH`).
Drove the app via `scripts/axprobe.swift`, `scripts/axclick.swift` (the `--last` flag was needed throughout, since the practice sheet's board squares share AX identifiers with the replay board rendered behind it), and `scripts/capture-window.swift`, with the app kept frontmost for sheet rendering.

Opened the real, fully-analyzed `MagnusCarlsen vs artin10862` game (164 plies) and its Report, which reconciled 4 real key-moment training cards with genuine 3-line MultiPV cached data (not synthetic seed data).

F6 acceptance: on card 1 (source ply 18, cached rank 1 `d8e7` cp52, rank 2 `e8g8` cp70, rank 3 `c5b4` cp151), submitted `c5b4` (the cached rank-3 move).
The UI showed `Inaccurate`, `Qe7 was stronger here`, `Engine loss: 99 centipawns` - not a blanket-accepted zero-loss strong move.
Screenshot at `/tmp/chessanto-v1-phase2-f6-fix-cached-line-graded.png` (local, uncommitted).

F1 acceptance: on card 2 (source ply 19, cached moves `e3c5`/`e1c1`/`e3d4`), submitted `a2a3`, a legal move matching none of the three cached lines, forcing a real engine search.
Feedback returned bounded and correct (`Inaccurate`, `Bxc5 was stronger here`, `Engine loss: 217 centipawns`), with the practice sheet never stuck on `Checking with the engine...`.

Batch-path acceptance: the first attempt (`Re-analyze` on the already-fully-analyzed `MagnusCarlsen` game) was a false positive - `analyze()`'s `analyzedPlies.contains(plyIndex)` skip means a re-analysis of an already-complete game persists nothing new, so seeing the row count unchanged at 164 proved nothing about the rewired path.
Corrected by finding a genuinely unanalyzed game and clicking `Analyze` (not `Re-analyze`) on it.
The first candidate (`Hikaru vs Casablanca`, gameId 2) turned out to have a pre-existing, unrelated PGN parse failure (`Couldn't parse this game's PGN: ChessKit.PGNParser.Error error 2`, matching the known parser edge case already logged in the 2026-07-17 M4 devlog section), which is why its `Analyze` button did nothing regardless of click method - not a regression.
`Hikaru vs lucumpe04` (gameId 6) loaded cleanly with zero prior analysis; clicking `Analyze` showed the progress indicator and cancel affordance, completed in ~28s, and `sqlite3` confirmed 222 freshly-written analysis rows - the rewired `searchOneShot`/`BoundedSearchSession` path genuinely exercised end to end, not a skip.
(Toolbar button clicks needed `axclick.swift ... --last`, since the practice sheet and the sidebar's "Analyzed" badge text both collide substring-wise with the toolbar button's own AX description; `axclickat.swift`'s raw screen-coordinate clicks were unreliable for this specific toolbar button regardless of window width.)

Coach FIFO acceptance: opened the Coach panel (which already had prior chat history from earlier seed data) and sent a new turn, `Is my king safe here?`, after the training evaluations and the full re-analysis above had already run in the same process.
The Coach responded normally (`chat-jump-22`), confirming the shared `coachEvaluateTail` FIFO still serializes narration/chat evaluations correctly after training evaluations have exercised the same chokepoint.
Screenshot at `/tmp/chessanto-v1-phase2-coach-fifo-after-training.png` (local, uncommitted).

The mate-grading fix (F7) was not exercised through native play, exactly as the plan anticipated - it needs a cached card whose position admits a mate that is not the rank-one move, which does not arise reliably from ordinary practice.
It is covered by the Step 5 tests only, stated here explicitly rather than claiming native coverage that was not obtained.

Database safety: after quitting the app, `md5` on the live sandbox database matched the pre-session backup exactly, confirming it was never mutated during this entire session.
All native mutation happened against the disposable QA copy.

### Documentation and review

Ran `mattpocock-skills:code-review` (the plugin-qualified two-axis review, not the built-in `/code-review`) over the full uncommitted diff against `3e395c1`, using `handoffs/NEXT-SESSION-V1-HARDENING-PHASE-2.md` as the spec axis.

Standards axis found two real issues, both fixed.
The `movetimeMilliseconds * 4 + 3000` deadline formula was an unnamed magic-number literal; extracted to `EngineService.deadlineMultiplier`/`deadlineFloorMilliseconds` with the resulting 3400/4400/11000/5000ms values spelled out in the doc comment.
The Step 5 mate-distance threshold silently changed from the old `classifyMate`'s `0...1 -> strong` to the new `0 -> strong` without being called out as intentional; added an inline comment distinguishing what the plan actually fixes (`shorter or equal distance is .strong`) from this evaluator's own judgement call for the non-strong tiers.
Everything else the Standards axis raised (the unused `EngineSearchError.engineUnavailable` case, the FEN-parsing helper duplicating logic `ChessCore` likely has internally, `EngineService`'s growing responsibility surface) was either spec-mandated (the error case exists because the plan's own code block defines it, even though nothing constructs it yet) or a legitimate judgement call already made deliberately, left as is.

Spec axis found all four exact test-name lists matched, the deadline formula matched, the "session installed before `go`" ordering rule matched, no scope creep against the plan's out-of-scope list, and no missing requirements.
It flagged three areas as "implementation looks right but under-specified relative to spec text," all already deliberate judgement calls: the mate-distance playable/inaccurate tiering (now commented, see above), the 200cp "clearly winning" threshold in the lost-forced-mate branch (now commented as this evaluator's own choice, not a plan-fixed number), and the `AnalysisEngine.setPosition` settle wait running unconditionally rather than only when a search was in flight.
The third is not a defect: the reproduced race specifically involved `isSearching` already being `false` by the time `setPosition` was entered, so gating the wait on that flag would reopen the exact race it exists to close - added an inline comment explaining this rather than changing the behavior.
Confirmed separately (via the corrected batch-analysis native test on `Hikaru vs lucumpe04`) that the unconditional wait does not meaningfully harm batch-analysis throughput: 222 fresh plies completed in ~28 seconds end to end.

Reran the full app suite (still 55/15) and `engine-smoke` (3 more clean runs) after applying the review's fixes, to confirm no regression from the comment/constant-naming changes.
