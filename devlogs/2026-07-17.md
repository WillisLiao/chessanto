# 2026-07-17

## M2 prep: live engine verification and execution plan

Goal of this session: turn `handoffs/NEXT-SESSION-M2.md` from a bootstrap
brief into a self-contained execution plan a fresh session can follow, and
de-risk its biggest unknown (no live Stockfish run had ever happened in
this repo) before that session starts.

### Read everything against real source

Re-read PLAN.md, all M1 code, and the actual checked-out sources of
chesskit-engine 0.7.0 and chesskit-swift (`.build/checkouts/`), which
surfaced four load-bearing facts the M1 handoff didn't know:

- chesskit-engine compiles Stockfish with `NNUE_EMBEDDING_OFF`, so the
  M1-era belief that "Stockfish 17 has an embedded default network" is
  wrong for this build. Worse, Stockfish 17 has no classical-eval
  fallback: a search with no net loaded prints "The engine will be
  terminated now." and calls `exit(EXIT_FAILURE)` in-process - it would
  have killed the app on first analysis. This is also (with the run-loop
  point below) why upstream's own Stockfish tests are disabled since 17.
- `Engine.start()` dup2()s the process's stdout into the engine's read
  pipe: `print()` stops reaching the terminal once the engine runs.
- Engine responses arrive via NSFileHandle run-loop notifications on the
  main thread: block the main thread and responses never arrive.
- The dup2 is process-wide, so only one live engine instance can exist
  per process - batch analysis and live scrubbing must share one engine.

Also confirmed: UCI scores are side-to-move relative (not white-relative),
`EngineResponse.Info.multipv` carries the MultiPV rank (which
`AnalysisEngine.EngineInfo` was dropping), `EngineCommand.go` supports
`movetime`, and chesskit-swift ships a public `EngineLANParser` (UCI ->
`Move`, castling already king-from/to) that M2 can use for PV -> SAN.

### Fixed and built

- `scripts/fetch-nnue.sh`: downloads both Stockfish 17 nets into
  `App/Resources/` and verifies them against the sha256 prefix embedded
  in their filenames (75MB + 3.5MB, gitignored via `*.nnue`).
- `project.yml`: the target's `resources:` key was silently dead -
  XcodeGen has no such key and ignores unknown keys, so the app never had
  a resources build phase (M1 had no resources, so nothing noticed).
  Replaced with a `sources` entry using `buildPhase: resources` plus
  excludes for Info.plist/entitlements; verified both nets now land in
  `Chessanto.app/Contents/Resources/`, where chesskit-engine's
  `Bundle.main` auto-lookup expects them.
- `EngineKit`: `EngineInfo` now forwards `multiPVRank`; added
  `setOption(name:value:)`; added the `engine-smoke` executable target -
  a live smoke run designed around the landmines above (stderr-only
  output, synchronous main + `RunLoop.main.run()`, detached engine task,
  explicit `EvalFile` setoptions since a CLI has no `Bundle.main` nets).

### Live verification (first real Stockfish run in this repo)

`swift run engine-smoke` passed on the first run:

- startpos, depth 15: `cp +38`, pv `e2e4 e7e5 g1f3 b8c6 f1b5 g8f6`,
  bestmove `e2e4`.
- White to move without his queen: `cp -725` - empirically pins the
  side-to-move sign convention all of M2's math must normalize from.
- Fool's mate, black to move: `mateIn 1`, bestmove `d8h4`.

EngineKit tests still pass and the full app builds with the regenerated
project.

### Plan written

`handoffs/NEXT-SESSION-M2.md` rewritten as a prescriptive execution plan:
verified engine facts, fixed conventions (plyIndex, white-perspective
storage, quality movetimes, cache rule, exact winP/accuracy formulas and
classification bands with worked numbers), and nine ordered steps each
with its own verification, ending in the PLAN.md acceptance pass.
`HANDOFF.md` corrected (NNUE claim) and updated.

## M2 execution: engine integration and move classification

Followed `handoffs/NEXT-SESSION-M2.md` step by step in a second session the
same day; every step's verification passed before moving to the next.

- **Persistence**: `AnalysisRecord` (mirrors the existing `analysis` table)
  plus `GameStore.saveAnalysis/analysis/analyzedPlyIndices/deleteAnalysis`,
  all async, delete-first replacement per `(gameId, plyIndex)`. 6 new tests.
- **EngineKit**: `go(movetimeMilliseconds:)`, and closed the stale-generation
  window from the prep plan (fact 8) - `setPosition` now waits for an
  in-flight search's terminating `bestmove` (`isSearching` flag flipped by
  the listen task) before bumping the generation, so nothing from the old
  search can arrive tagged with the new one. `EngineInfo` gained a public
  memberwise init (needed for structural tests outside the package).
  `engine-smoke` extended with a movetime-based search assertion; still
  exits 0 (`OK: live Stockfish verified...`).
- **ChessCore**: `ChessGame.uciMove(at:)` turned out to be a one-liner -
  chesskit-swift's `Move.lan` already produces exact UCI notation including
  castling and promotion letters, so no new logic was needed there.
  `ChessGame.sanLine(fromUCI:startingFEN:)` replays a UCI line via
  `EngineLANParser.parse` + `Board.move`/`completePromotion`. 4 new tests,
  including a crafted promotion PGN and the smoke run's own PV round-tripped
  into SAN.
- **AnalysisKit**: `WinProbability`, `MoveClassification`,
  `MoveClassifier`, `Accuracy` - pure math, no engine/DB imports, exactly
  the formulas fixed in the prep plan. 17 tests, including the worked
  example (+50cp/-250cp -> Mistake), a Black-to-move blunder (proving the
  classifier correctly flips perspective rather than reading white-relative
  cp directly), missed-win firing/not-firing, and mate-score plies. Band
  boundary tests use bisection to invert the winP formula back to cp, with
  a small margin (0.5 rather than the plan's 0.01) away from each threshold
  since integer-cp resolution can't hit exact fractional win probabilities.
- **App**: `EngineService` (`@MainActor`, one `AnalysisEngine` for the whole
  app, live infinite analysis and batch analysis mutually exclusive per
  engine facts 5/6). Batch search completion uses a
  `withTaskCancellationHandler` around a `CheckedContinuation` that the
  routing loop resumes on the terminating bestmove (including
  `bestmove "(none)"`), with `engine.stop()` on cancellation so the
  continuation still resolves before the batch loop rethrows. `BatchCollector`
  and `LiveGenerationFilter` factored out as plain structs, unit-tested
  without a live engine (9 App tests total). Wired `EvalBarView`,
  `EvalGraphView`, move-list classification badges, an Analyze/Re-analyze
  toolbar with a quality picker and cancellable progress, and a
  display-only MultiPV lines panel into `GameReplayView`/
  `GameReplayViewModel`. Live analysis starts on ply change (200ms debounce)
  and stops on view disappear; the eval bar/lines panel only trust
  `EngineService.liveEvaluation` when its `fen` matches the ply being shown,
  falling back to the cached DB row otherwise - this is what makes the
  no-stale-eval acceptance drill pass.

### Real E2E verification (not just tests)

Screen Recording permission wasn't available in this environment, so
verification used `osascript`/System Events accessibility introspection
(`entire contents of window`) plus `cliclick` instead of screenshots -
still driving the actual built Release app, not mocks:

- Selected the M1-era `MagnusCarlsen vs artin10862` game already in the
  sandboxed container DB, clicked Analyze (Standard quality), watched the
  toolbar button go Analyze -> Cancel -> Re-analyze in well under 30s.
- `sqlite3` against the sandboxed DB
  (`~/Library/Containers/com.chessanto.app/Data/Library/Application
  Support/Chessanto/chessanto.sqlite`) showed 164 analysis rows (55 plies
  x up to 3 MultiPV ranks) with sane depths/scores; spot-checked several
  plies for the white-perspective sign convention.
- Quit and relaunched the app, selected the same game: accuracy summary
  ("White 93.8 · Black 90.6") and the Re-analyze button (not Analyze)
  appeared immediately, before any engine interaction - cache-instant-load
  confirmed.
- Stepped through 8 plies via the Next-move button; the eval bar updated
  each time to a value close to (but not identical to, since live search
  only ran ~200ms vs. the cached Standard-quality search) that ply's cached
  DB row - confirming live re-analysis per position rather than a stale
  carryover.
- Accessibility introspection couldn't reach into the move list's
  `LazyVStack`/`ScrollView` (SwiftUI exposes it as an opaque provider group
  to System Events), so individual move badges weren't directly verified
  this way; the accuracy summary appearing correctly is strong indirect
  evidence since both are derived from the same `deriveClassifications()`
  call.

All package tests (ChessCore, EngineKit, Persistence, AnalysisKit) and all
App/ChessantoTests pass; `engine-smoke` exits 0; `xcodebuild build` and
`test` both succeed.

## M3: Exploration Mode (variation play, tree, lines-panel adoption)

Same day, third session. Followed `handoffs/NEXT-SESSION-M3.md`'s bootstrap
approach: re-derived the design from chesskit-swift's actual `MoveTree`
source rather than trusting assumptions.

### Design decision: don't fight chesskit's `MoveTree` internals

`MoveTree.Node.children` is internal to ChessKit and there is no public
API to enumerate branch alternatives or delete a subtree. Rather than
trying to mutate chesskit's tree, the app now owns the tree shape itself:

- `ChessGame` gained `allIndices`, `isMainline(_:)`, `parent(of:)` (derived
  from the existing public `history(for:)`), and `mainlineAncestor(of:)` -
  enough to answer "what's the parent of this index" and "where does this
  variation rejoin the mainline" without touching ChessKit internals.
- `Persistence` gained `VariationRecord` (one row per played variation
  move: `parentPlyIndex` for a row branching directly off the mainline,
  `parentVariationId` pointing at the preceding row otherwise - a plain
  parent-pointer forest) and `GameStore.insertVariationMove` /
  `variations(gameId:)` / `deleteVariation(id:)`. Deleting a row cascades
  to its whole subtree via the schema's existing `ON DELETE CASCADE` on
  `parentVariationId` - confirmed with a test that deletes a middle node
  of a 3-deep chain and asserts a sibling survives.
- `GameReplayViewModel` rebuilds this tree in memory on load by replaying
  each `VariationRecord` onto `chessGame` in insertion order (parents are
  always inserted before children, since you can't play a move before its
  parent exists) and keeps `variationChildren`/`variationNodes` alongside
  the existing mainline `moveIndices`. New moves are persisted the same
  way, one row per move, immediately after being played - matching M2's
  per-ply save pattern so quit/crash never loses more than the move in
  flight. A move that happens to match an existing mainline/variation
  continuation is recognized and not re-persisted as a duplicate branch
  - except see the known gap below.
- `ChessGame.playMove(from:to:at:)` now auto-completes promotion (default
  queen; `PromotionKind` enum) - needed since board taps are the first
  real move-input path in the app - and gained `isPromotion(from:to:at:)`
  for a future picker.
- Board taps: `BoardView` squares became real `Button`s (was
  `.onTapGesture`) with click-to-select/click-to-move and legal
  destination highlighting - see the accessibility note below for why
  this mattered beyond just code style.
- Move list: recursive `variationRows(startingAt:depth:)` flattens the
  tree so the principal (first-played) continuation stays at its parent's
  depth while any additional branch at the same point nests one level
  deeper - the standard PGN variation-rendering shape. Delete removes a
  row and everything explored from it (`GameReplayViewModel.removeSubtree`
  mirrors the DB cascade in memory). "Back to game" jumps to
  `mainlineAncestor(of: currentIndex)`. Promote/collapse from PLAN.md's
  bullet are **not** implemented this milestone - out of scope for the
  accept criterion, noted below as a gap.
- `LinesPanelView` is no longer display-only: clicking a line adopts it
  (`ChessGame.sanLine(fromUCI:startingFEN:)` -> `adoptLine(sanMoves:)`,
  which plays each move as a nested variation branch one deeper than the
  last).
- `currentFEN`/`currentEvalDisplay` were mainline-`moveIndices`-array
  lookups in M2 (fine, since nothing but the mainline existed yet) -
  generalized to read straight off `chessGame.fen(at: currentIndex)` so
  the eval bar and live engine tracking work identically for variation
  positions, which is what actually makes "eval bar tracks every explored
  position live" true rather than just true-by-coincidence on the
  mainline.

### Real E2E verification, and what it caught

Screen Recording still isn't available in this environment. The
2026-07-17 earlier-session `osascript`/System Events method mostly
carried over, with one important correction: **raw pixel-coordinate
clicks (`cliclick`, and System Events' `click at {x,y}`) are blocked in
this sandbox** (`-25211`, "not allowed assistive access") - only
AX-element-reference clicks (`click item N of buttons of ...`) are
permitted. That single fact drove a real product fix, not just a test
workaround: `BoardView`'s squares used `.onTapGesture`, which (unlike
`Button`) exposes no AX action at all, so board taps were fully
undrivable and - worse - would have been invisible to VoiceOver too.
Converting squares to real `Button`s fixed both. `LinesPanelView`'s
adopt-a-line buttons still don't expose as `AXButton` despite an
identical `Button` + `.buttonStyle(.plain)` shape to the (working)
move-list buttons; added `.accessibilityElement(children: .combine)` as
a mitigation but this needs re-verification once Screen Recording (or a
proper XCUITest target) is available - the automation for this milestone
exercised the lines-panel adopt path only indirectly, not through a
confirmed click.

The introspection also caught a real layout bug before it shipped:
`MoveListView` nested each move's variation children inside
`if let san = viewModel.san(at: index)`, so variations branching directly
off the game's start position (ply 0, which has no SAN) never rendered
or were deletable. Fixed by hoisting the variation `ForEach` out of that
`if`.

With both fixes, the full accept criterion was driven end-to-end through
the built Release app (`MagnusCarlsen vs artin10862`, the M1/M2 fixture
game already in the sandboxed container DB), against the real per-square
`AXIdentifier`s (`square-e4`, computed 64 button indices from the a8/h1
corner pixel positions once, reused throughout):

- From the start position, played 1.d4 d5 2.c4 e6 3.Nc3 by clicking
  through five board-square pairs; `sqlite3` against the sandboxed DB
  showed exactly the expected 5-row parent-pointer chain
  (`d4 -> d5 -> c4 -> e6 -> Nc3`, each `parentVariationId` pointing at the
  previous row).
- Stepped back two plies (chevron.left x2, confirmed via raw arithmetic
  stepping working correctly within a single variation branch) to the
  position after 3.c4, played Nf6 as an alternative to e6 - DB gained a
  6th row with `parentVariationId` pointing at the `c4` row and
  `orderIndex 1` (sibling to `e6`'s `orderIndex 0`), confirming real
  nested-branch-point semantics, not just a linear chain.
- Read the live "Evaluation +0.7, White win probability 56 percent" text
  for this freshly-explored position - a plausible engine value distinct
  from every other position checked this session, confirming live
  analysis really does track exploration positions and not just the
  mainline.
- Clicked the `delete-variation-Nf6` trash button in the move list; the
  DB row disappeared while the 5-row main chain stayed intact (cascade
  correctly scoped to just that node's subtree).
- Quit and relaunched the app, reselected the same game: `sqlite3` showed
  the same 5 surviving rows, and the move list's live-in-app
  `delete-variation-*` identifiers matched them exactly (`d4`, `d5`,
  `c4`, `e6`, `Nc3`) - confirming the reconstruction-on-load replay logic
  (not just the DB row survival) actually works.
- One coincidental finding along the way: playing a board move that
  happens to match the loaded game's real next mainline move (e.g. 1.e4
  on this game) currently still creates a redundant variation row instead
  of being recognized as "already the mainline" - chesskit's `Move`
  equality apparently doesn't consider a freshly-constructed `Move` equal
  to the one parsed from the game's PGN even when they're the same move.
  Harmless (the game still plays on correctly) but not elegant; logged as
  a known gap below rather than fixed this session.

All package tests (ChessCore - now including nested-variation and
auto-promotion tests - Persistence, with cascading-subtree-delete tests,
AnalysisKit, ChessComKit, CoachKit, EngineKit) and `xcodebuild build`/
`test` for the App target all pass.

### Known gaps for a future session

- Promote-variation and collapse controls (PLAN.md's M3 bullet) are not
  built - only delete, "back to game", and click-to-jump.
- No promotion picker UI - `ChessGame.playMove(from:to:at:)` always
  auto-promotes to queen. `isPromotion(from:to:at:)` exists for when a
  picker gets built.
- Replaying a variation move that happens to equal the real mainline
  continuation creates a redundant variation branch instead of
  recognizing the mainline (see above) - worth a real fix (likely
  comparing SAN/UCI rather than relying on ChessKit's `Move` equality)
  before it confuses users exploring near the actual game moves.
- `LinesPanelView`'s adopt-line buttons' accessibility exposure is
  unconfirmed (see above) - re-verify once Screen Recording or an
  XCUITest target is available.
