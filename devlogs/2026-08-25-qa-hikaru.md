# 2026-08-25 - QA hardening: Hikaru games (in progress, scan not yet run)

Branch: `qa/hikaru-games` (worktree `../chessanto-qa-hikaru`). This session
resumed a prior instance's uncommitted work per
`handoffs/NEXT-SESSION-QA-HIKARU-RESUME.md`, and was cut short before the
full-archive scan could run. See `handoffs/NEXT-SESSION-QA-HIKARU-FINISH.md`
for exactly how to continue.

## Harness disposition

The prior instance left an AnalysisKit package executable (`hikaru-qa`) that
re-implemented `ReportBuilding.buildInput` inline. That is the same redundant
shape the Carlsen session documented and deleted: it cannot reach the real
end-user path (`GameReplayViewModel(record:store:)` ->
`ReportBuilding.buildInput`/`buildReport`) that this task's scope demands.
Committed as a checkpoint first (`93bc961`), then deleted it and restored
`Packages/AnalysisKit/Package.swift` to main's version.

In its place this session ported the proven Carlsen pattern:

- `HikaruQAScanTests.swift` (new, in `App/Tests/ChessantoTests/`): mirrors
  `CarlsenQAScanTests`. Reads cached archive JSON from
  `HIKARU_QA_ARCHIVES_DIR`, loads every game through the real
  `GameReplayViewModel(record:store:)` path, checks FEN shape at every ply,
  compares the final FEN against chess.com's `CurrentPosition` tag (board and
  active color only), then builds synthetic single-line analysis rows and runs
  `ReportBuilding.buildInput` + `ReportBuilding.buildReport` per game.
  Skips and separately counts no-PGN games and Variant-tagged games, both out
  of product scope per PLAN.md.
- `chesscom-smoke --all --out-dir <dir>` (ported from `qa/carlsen-games`):
  fetches every monthly archive once and caches decoded games as JSON so
  rescans never touch the network.
- `ChessComGame`/`ChessComPlayer` are now `Codable` for that cache (identical
  to the Carlsen branch).

## Archives fetched

All 152 monthly public archives of chess.com user `hikaru`: 70,182 games,
cached as JSON under
`/Users/willis/.gemini/antigravity-cli/brain/ed28d818-b105-4ad9-9eaf-a0314594e75b/scratch/hikaru_archives`
(152 files). Rescans need no network.

## What was found and fixed

1. **One PGN-less game poisoned decoding of an entire monthly archive
   (real end-user fetch failure).** chess.com serves bughouse and other
   partner-variant games with no `pgn` key at all (first seen in the 2016/09
   archive, game index 25 of 114; 20 such games in that month alone).
   `ChessComGame.init(from:)` required `pgn` unconditionally, so decoding the
   archive threw `keyNotFound: 'pgn'` and the whole month failed - on the app
   path that surfaces as "chess.com returned data Chessanto didn't understand"
   for any Hikaru-like account that plays variants. Fix in the shared layer:
   `decodeIfPresent ?? ""` with a comment naming why. Downstream,
   `GameLibrary.importPGN` already rejects empty/unparseable PGNs gracefully,
   so import degrades per game instead of per month. Regression test added:
   `missingPGNDoesNotPoisonArchiveDecoding` decodes a two-game response where
   the first has no `pgn` key and asserts both survive with correct fields.
   Packages/ChessComKit: 5 tests (was 4).

## Verification status (honest state)

Ran and passed before wrap-up:

```
swift test --package-path Packages/ChessCore      -> Test run with 57 tests in 1 suite passed after 1.381 seconds.
swift test --package-path Packages/ChessComKit    -> Test run with 5 tests in 1 suite passed after 0.156 seconds.
swift test --package-path Packages/AnalysisKit    -> Test run with 195 tests in 6 suites passed after 59.795 seconds.
xcodegen generate                                 -> Created project at .../chessanto-qa-hikaru/Chessanto.xcodeproj
```

Not yet done, in order:

- `xcodebuild ... build` was started but interrupted by session end; rerun it.
- The full-archive scan itself has NOT run:
  `HIKARU_QA_ARCHIVES_DIR=<dir> xcodebuild ... -only-testing:ChessantoTests/HikaruQAScanTests`
- Any ChessCore parser failures the scan surfaces have not been triaged. Note:
  this branch forked from main WITHOUT the Carlsen branch's three PGN fixes
  (en passant after a replayed double push, silently-wrong disambiguated
  captures, rank/square disambiguation plus castling-with-check suffix), all
  unmerged on `qa/carlsen-games`. If Hikaru's games fail in those same shapes,
  porting the identical fix is preferred over re-deriving it - identical
  content merges cleanly later.
- Full verification bar (all three package suites plus xcodebuild build/test)
  and the devlog quote of the final "Test run with N tests in M suites" line.

## Scan run and Carlsen fix port (same day, later)

The full-archive scan finally ran to completion in the app-suite Debug build:

```
HIKARU QA SCAN SUMMARY
Total games scanned: 70182
Skipped no-PGN games (bughouse and other partner variants): 92
Skipped variant games (out of product scope): 1454
Total plies replayed: 5510993
PGN parse failures: 4503
FEN mismatches: 523
Report building failures: 0
Zero key moment reports (logged, not asserted): 64104
Other issues: 0
```

(The run took about 73 minutes wall clock; an earlier attempt appeared to crash at
46 seconds inside the full suite but an isolated rerun of only this test showed no
crash over a full hour - the earlier death did not reproduce and is treated as
environmental.)

Triage: every sampled parse failure is `PGNCompatibility.Error error 0` on games
containing disambiguated piece captures such as `28... Nexd5` in
live/692724102 - exactly the upstream defect already fixed unmerged on
`qa/carlsen-games`, which this branch forked without, exactly as the bootstrap
anticipated. The FEN mismatches are the same root cause expressing silently:
a wrong-knight move diverges the whole replay, so the final FEN drifts from
chess.com's `CurrentPosition` tag.

Fix: ported the identical content from `qa/carlsen-games` verbatim -
`PGNCompatibility.requiresFallback(for:)` + rewritten `parseSAN`
(disambiguation by file/rank/square, en-passant construction, suffix retry)
+ `ChessGame.init(pgn:)` routing, plus its 7 regression tests. Identical
content merges cleanly with the Carlsen branch during integration.

Validation against real failing data (scratch harness against this branch's
ChessCore):

```
692724102.pgn: OK 70 plies     (was PGNCompatibility.Error error 0)
692734556.pgn: OK 73 plies
737800981.pgn: OK 60 plies
737803005.pgn: OK 86 plies
737807296.pgn: OK 68 plies
744127123.pgn: OK 58 plies
744134092.pgn: OK 106 plies
```

And for FEN mismatches, all 5 sampled games now TAG-MATCH after the port,
including live/737789994 whose final mainline FEN now equals chess.com's tag
byte-for-byte (`r3r3/R4pkp/2b3p1/1N6/4P3/6P1/5PBP/n5K1 w - - 0 26`).

Verification status:

- Packages/ChessCore: `Test run with 64 tests in 1 suite passed` (57 prior + 7 ported).
- Full app-suite re-run for the final "Test run with N tests" line: queued;
  note it costs about 73 minutes because the scan auto-runs whenever the
  cached archives exist. Gating the scan behind an explicit env var at
  integration time is recommended.
